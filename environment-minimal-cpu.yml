# Minimal Conda Environment for CPU
# This installs ONLY system-level dependencies via conda
# Python packages are installed via pip afterwards (much faster!)

name: agents_unplugged
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  # Python
  - python=3.11

  # CPU-only PyTorch (much smaller, no CUDA)
  - pytorch::pytorch>=2.0,<2.5
  - pytorch::torchvision
  - pytorch::torchaudio
  - cpuonly

  # Core scientific computing
  - numpy>=1.26,<2.0
  - pandas>=2.0,<3.0

  # Jupyter
  - jupyter
  - ipykernel
  - notebook

  # Pip (to install remaining packages)
  - pip

  # Note: All other packages will be installed via pip
  # This keeps conda resolution fast and prevents memory issues
